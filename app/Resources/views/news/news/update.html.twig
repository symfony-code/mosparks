{% extends 'base.html.twig' %}

{% block body %}
    {% set breadcrumbs = {'breadcrumbs' : [{'url': path('newsIndex'), 'label': 'Новости'},{'label': 'Редактировать'}]} %}
    {% include 'parts/breadcrumbs.html.twig' with breadcrumbs %}
    {{ form_start(form) }}
    {{ form_widget(form) }}
    {{ form_end(form) }}
{% endblock %}